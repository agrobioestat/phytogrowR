% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_instant_rates.R
\name{calc_instant_rates}
\alias{calc_instant_rates}
\title{Calculate Instantaneous Growth Rates from Fitted Curves}
\usage{
calc_instant_rates(
  biomass_fit,
  leaf_area_fit = NULL,
  leaf_mass_fit = NULL,
  ground_area = NULL,
  central = TRUE,
  epsilon = NULL
)
}
\arguments{
\item{biomass_fit}{A \code{phytogrow_fit} object for total biomass.}

\item{leaf_area_fit}{Optional \code{phytogrow_fit} object for leaf area.}

\item{leaf_mass_fit}{Optional \code{phytogrow_fit} object for leaf biomass.}

\item{ground_area}{Either \code{NULL}, a single numeric value, or a column name
available in the original fit data.}

\item{central}{Logical; use central difference for numerical derivatives.}

\item{epsilon}{Optional positive offset for \code{log(W)} when values are near zero.}
}
\value{
A tibble with fitted values and instantaneous rates.
}
\description{
Derive instantaneous rates from smooth growth curves fitted with
\code{\link[=fit_growth_curve]{fit_growth_curve()}}.
}
\details{
Computed rates:
\itemize{
\item instantaneous \code{AGR = dW/dt}
\item instantaneous \verb{RGR = d(log W)/dt}
\item instantaneous \code{NAR = (1/LA) * dW/dt}
\item instantaneous \code{LAR = LA/W}
\item instantaneous \code{SLA = LA/leaf_mass} (if leaf mass fit is provided)
\item instantaneous \code{CGR = (1/ground_area) * dW/dt}
\item instantaneous \code{ALGR = dLA/dt} and \verb{RLGR = d(log LA)/dt} (if leaf area fit is provided)
\item instantaneous \code{LAI = (LA/10000)/ground_area}
}
}
\examples{
dat <- prepare_growth_data(growth_wide_example)
fit_w <- fit_growth_curve(dat, response = "total_biomass_g", group_cols = "treatment")
fit_la <- fit_growth_curve(dat, response = "leaf_area_cm2", group_cols = "treatment")
inst <- calc_instant_rates(fit_w, leaf_area_fit = fit_la)
utils::head(inst)
}
\references{
Hunt, R. (1990). \emph{Basic Growth Analysis}. Unwin Hyman, London.

Benincasa, M. M. P. (2003). \emph{Plant Growth Analysis: Basic Concepts}.
FUNEP, Jaboticabal.
}
