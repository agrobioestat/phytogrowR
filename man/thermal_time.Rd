% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thermal_time.R
\name{thermal_time}
\alias{thermal_time}
\title{Accumulated Thermal Time (Growing Degree Days)}
\usage{
thermal_time(
  data,
  date = "date",
  tmin = "tmin",
  tmax = "tmax",
  tmean = NULL,
  t_base = 10,
  t_upper = NULL,
  method = c("simple", "cutoff", "modified"),
  group_var = NULL,
  origin = NULL
)
}
\arguments{
\item{data}{A data frame with one row per day.}

\item{date}{Name of the date column (a \code{Date}, or anything coercible).}

\item{tmin}{Name of the daily minimum temperature column. Optional when
\code{tmean} is supplied.}

\item{tmax}{Name of the daily maximum temperature column. Optional when
\code{tmean} is supplied.}

\item{tmean}{Name of a daily mean temperature column. When \code{NULL}, the mean
is computed as \code{(tmin + tmax) / 2}.}

\item{t_base}{Base temperature below which development is taken to stop, in
the same unit as the temperatures.}

\item{t_upper}{Optional upper threshold. Ignored when \code{method = "simple"}.}

\item{method}{Accumulation method: \code{"simple"}, \code{"cutoff"} or \code{"modified"}.
See Details.}

\item{group_var}{Optional grouping column (site, season, sowing date, ...).
Accumulation restarts within each group.}

\item{origin}{Optional date at which accumulation starts (sowing or
emergence). Days before it contribute zero. When \code{NULL}, accumulation starts
at the first date available in each group.}
}
\value{
The input data with three columns added: \code{t_mean_effective} (the
daily mean actually used, after any capping), \code{gdd} (the daily contribution)
and \code{thermal_time} (the running total).
}
\description{
Converts a daily temperature series into daily and accumulated thermal time,
so that growth analysis can be run against degree-days instead of calendar
days.
}
\details{
Writing \eqn{T_{mean}} for the daily mean temperature, the three methods are:

\describe{
\item{\code{"simple"}}{\eqn{GDD = \max(0,\; T_{mean} - T_{base})}. The upper
threshold is ignored. This is the standard textbook formulation and the
right default when temperatures stay well below the upper limit of the
species.}
\item{\code{"cutoff"}}{\code{tmax} is capped at \code{t_upper} before the mean is taken,
so hot afternoons stop contributing. Use it where daytime temperatures
exceed the optimum for the crop.}
\item{\code{"modified"}}{\code{tmin} is additionally raised to \code{t_base} before the
mean is taken, in addition to the \code{"cutoff"} cap. This is the form used
in much of the maize and wheat literature.}
}

All three are approximations of an integral over the daily temperature
course, computed from two readings a day. They diverge whenever a day
crosses \code{t_base} or \code{t_upper}, so the method should be reported alongside
any thermal time that is published.

To use the result, join it onto the growth data by date and map the
accumulated column as \code{time}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{tt <- thermal_time(weather, date = date, tmin = tmin, tmax = tmax, t_base = 10)
growth <- merge(growth, tt[, c("date", "thermal_time")], by = "date")
dat <- prepare_growth_data(growth, mapping = list(time = "thermal_time"))
}\if{html}{\out{</div>}}
}
\examples{
weather <- data.frame(
  date = as.Date("2024-01-01") + 0:29,
  tmin = 12 + 5 * sin(seq(0, 3, length.out = 30)),
  tmax = 26 + 5 * sin(seq(0, 3, length.out = 30))
)

tt <- thermal_time(weather, date = date, tmin = tmin, tmax = tmax, t_base = 10)
head(tt)

# Capping hot days at 30 degrees accumulates less thermal time
tt2 <- thermal_time(
  weather, date = date, tmin = tmin, tmax = tmax,
  t_base = 10, t_upper = 30, method = "cutoff"
)
tail(tt2$thermal_time, 1) < tail(tt$thermal_time, 1)
}
\seealso{
\code{\link[=prepare_growth_data]{prepare_growth_data()}} for mapping the result onto \code{time}.
}
