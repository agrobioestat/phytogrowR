% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_phytogrow_app.R
\name{run_phytogrow_app}
\alias{run_phytogrow_app}
\title{Run the phytogrowR Shiny App}
\usage{
run_phytogrow_app(...)
}
\arguments{
\item{...}{Additional arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
Invisibly returns the app object. Called for its side effect of
starting the interactive session.
}
\description{
Launches the interactive app shipped in \code{inst/shiny/phytogrowRApp}.
}
\details{
The app and its widgets rely on packages listed in \code{Suggests}
(\code{shiny}, \code{bslib}, \code{DT}, \code{readr}) rather than \code{Imports}, so the analytical
core of \code{phytogrowR} installs without them. The availability of every
optional package is verified here, before the app directory is handed to
\code{\link[shiny:runApp]{shiny::runApp()}}.

The interface is organised as a Bootstrap 5 dashboard: a collapsible
sidebar for the controls, a row of summary boxes for the design, and one
card-based tab per analysis module. Five in-package example datasets are
available from the sidebar, so every module can be exercised without any
external file.
}
\examples{
\dontrun{
run_phytogrow_app()
}
}
