% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_growth_curves.R
\name{compare_growth_curves}
\alias{compare_growth_curves}
\title{Formal Test of Coincidence of Growth Curves Between Treatments}
\usage{
compare_growth_curves(
  data,
  response = "total_biomass_g",
  time_col = "time",
  group_var = "treatment",
  model = c("poly", "exponential", "logistic", "gompertz", "richards"),
  degree = 2,
  log_response = FALSE,
  epsilon = NULL,
  test = c("both", "F", "LRT"),
  pairwise = FALSE,
  p_adjust_method = "BH",
  center_time = TRUE
)
}
\arguments{
\item{data}{A data frame with time, response and grouping columns. Raw data
is accepted; run \code{\link[=prepare_growth_data]{prepare_growth_data()}} first to obtain the standard
column names.}

\item{response}{Response column, for example \code{"total_biomass_g"} or
\code{"leaf_area_cm2"}.}

\item{time_col}{Numeric time column.}

\item{group_var}{Grouping column holding the treatments to be compared.}

\item{model}{Functional form fitted to every treatment: \code{"poly"}
(polynomial in time, the default and most robust), \code{"exponential"},
\code{"logistic"}, \code{"gompertz"} or \code{"richards"}.}

\item{degree}{Polynomial degree when \code{model = "poly"}. Using \code{degree = 2}
together with \code{log_response = TRUE} reproduces the classical log-quadratic
functional growth analysis.}

\item{log_response}{Logical; fit on the natural log of the response. On this
scale the first derivative of the curve is the relative growth rate.}

\item{epsilon}{Optional positive offset added before the log transform, for
data containing zeros.}

\item{test}{Which test(s) to report: \code{"both"} (default), \code{"F"} or \code{"LRT"}.}

\item{pairwise}{Logical; also run the coincidence test for every pair of
treatments.}

\item{p_adjust_method}{Multiplicity adjustment applied to the pairwise
p-values, passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{center_time}{Logical; centre time at its mean before fitting the
polynomial. This improves the conditioning of the design matrix and changes
neither the fitted values nor any test statistic, only the meaning of the
intercept.}
}
\value{
An object of class \code{phytogrow_curve_test}: a list whose main
elements are \code{tests} (tibble of hypotheses and statistics), \code{pairwise}
(tibble or \code{NULL}), \code{parameters} (per-treatment estimates), \code{models} and
fitting metadata. Use \code{\link[=tidy]{tidy()}}, \code{summary()} or \code{plot()} on the result.
}
\description{
Tests whether a single growth curve is enough to describe every treatment,
or whether each treatment needs its own curve, by comparing two nested
models with an F test and/or a likelihood ratio test.
}
\details{
Let the response of observation \eqn{i} in treatment \eqn{g} follow a common
functional form with treatment-specific parameters,
\eqn{y_{ig} = f(t_{ig}; \theta_g) + e_{ig}}, with
\eqn{e_{ig} \sim N(0, \sigma^2)}. The reduced model imposes
\eqn{\theta_1 = \ldots = \theta_k} (coincident curves, \eqn{p_0 = p}
parameters); the full model leaves every \eqn{\theta_g} free
(\eqn{p_1 = kp} parameters). Writing \eqn{RSS} for the residual sum of
squares of each fit and \eqn{n} for the total number of observations:

\deqn{F = \frac{(RSS_0 - RSS_1)/(p_1 - p_0)}{RSS_1/(n - p_1)}
  \sim F_{p_1 - p_0,\, n - p_1}}

\deqn{G = -2(\ell_0 - \ell_1) = n \log(RSS_0/RSS_1) \sim \chi^2_{p_1 - p_0}}

The F test is exact under normality for \code{model = "poly"} (a linear model)
and approximate for the non-linear families; the likelihood ratio test is
asymptotic in every case. This is the nested-model, or identity-of-models,
test of Chow (1960) and Graybill (1976).

Beyond the global \code{"coincidence"} hypothesis the output decomposes the
difference between treatments:
\itemize{
\item \code{model = "poly"} adds \code{"level"} (equal intercepts given a common shape)
and \code{"shape"} (equal time coefficients, i.e. parallel curves);
\item the non-linear families add one \code{"parameter"} row per parameter, obtained
by restricting that single parameter to be common while the others stay
treatment-specific. This isolates whether treatments differ in final size
(\code{Asym}), timing (\code{xmid}, \code{b}) or rate (\code{scal}, \code{c}, \code{k}, \code{r}).
}

The full non-linear model is fitted independently within each treatment and
the residual sums of squares are pooled, which is algebraically equivalent
to the joint indexed model but numerically far more stable.

Because a single residual variance is assumed for all treatments, inspect
the residuals before trusting a borderline p-value; strongly heteroscedastic
biomass data usually calls for \code{log_response = TRUE}.
}
\examples{
dat <- prepare_growth_data(growth_wide_example)

# Classical log-quadratic functional growth analysis
ct <- compare_growth_curves(
  dat,
  response = "total_biomass_g",
  group_var = "treatment",
  model = "poly",
  degree = 2,
  log_response = TRUE,
  epsilon = 1e-6
)
ct
tidy(ct)
}
\references{
Chow, G. C. (1960). Tests of equality between sets of coefficients in two
linear regressions. \emph{Econometrica}, 28(3), 591-605.
\doi{10.2307/1910133}

Graybill, F. A. (1976). \emph{Theory and Application of the Linear Model}.
Duxbury Press, North Scituate.

Poorter, H., and Garnier, E. (1996). Plant growth analysis: an evaluation of
experimental design and computational methods. \emph{Journal of Experimental
Botany}, 47(9), 1343-1351. \doi{10.1093/jxb/47.9.1343}

Hunt, R., Causton, D. R., Shipley, B., and Askew, A. P. (2002). A modern
tool for classical plant growth analysis. \emph{Annals of Botany}, 90(4),
485-488. \doi{10.1093/aob/mcf214}
}
\seealso{
\code{\link[=compare_growth]{compare_growth()}} for index-by-index pairwise contrasts and
\code{\link[=fit_growth_curve]{fit_growth_curve()}} for the smoothing-based functional approach.
}
