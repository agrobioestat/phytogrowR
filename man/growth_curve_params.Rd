% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth_curve_params.R
\name{growth_curve_params}
\alias{growth_curve_params}
\title{Biologically Interpretable Parameters of a Fitted Growth Curve}
\usage{
growth_curve_params(object, plateau_tol = 0.05)
}
\arguments{
\item{object}{An object of class \code{phytogrow_fit}.}

\item{plateau_tol}{Fraction of the maximum absolute growth rate below which
the curve is considered to have plateaued at the last observed time.}
}
\value{
A tibble with one row per group and the landmark columns described
above, preceded by the grouping columns of the fit.
}
\description{
Extracts the landmarks an experiment is usually designed to estimate -
when growth peaked, how fast it was then, how long the active phase lasted,
and whether the curve plateaued at all - from an object returned by
\code{\link[=fit_growth_curve]{fit_growth_curve()}}.
}
\details{
All quantities are read off the prediction grid, so one implementation
serves every method the package can fit. The reported \code{n_grid} is the
resolution behind them: a coarse grid rounds \code{t_inflection} to the nearest
grid step, so increase \code{n_grid} in \code{\link[=fit_growth_curve]{fit_growth_curve()}} when the timing
itself is the result of interest.

The returned columns are:

\describe{
\item{\code{asymptote}, \code{asymptote_source}}{Final size. For \code{logistic},
\code{gompertz} and \code{richards} fits this is the model's \code{Asym} parameter; for
a smoother there is no asymptote parameter and the maximum fitted value
stands in, which \code{asymptote_source} records. Treat the second case as a
lower bound, not as an estimated plateau.}
\item{\code{t_inflection}, \code{y_inflection}, \code{max_agr}}{Time of maximum absolute
growth rate, the size reached there, and that maximum rate. For a
sigmoid curve this is the inflection point.}
\item{\code{max_rgr}, \code{t_max_rgr}}{Maximum relative growth rate and when it
occurs; typically early, well before \code{t_inflection}.}
\item{\code{t10}, \code{t50}, \code{t90}}{First times at which 10\\%, 50\\% and 90\\% of the
asymptote are reached, linearly interpolated between grid points.}
\item{\code{active_phase}}{\code{t90 - t10}, the duration of the main growth period.}
\item{\code{plateau_reached}}{\code{TRUE} when the growth rate at the last time has
fallen below \code{plateau_tol} of its maximum. When \code{FALSE}, the curve was
still rising when the experiment ended and \code{asymptote} is not an
estimate of final size.}
}
}
\examples{
dat <- prepare_growth_data(growth_wide_example)
fit <- fit_growth_curve(
  dat,
  response = "total_biomass_g",
  group_cols = "treatment",
  method = "logistic"
)
growth_curve_params(fit)
}
\seealso{
\code{\link[=fit_growth_curve]{fit_growth_curve()}}, \code{\link[=calc_instant_rates]{calc_instant_rates()}},
\code{\link[=compare_growth_models]{compare_growth_models()}}.
}
