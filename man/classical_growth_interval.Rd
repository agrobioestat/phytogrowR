% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classical_growth_interval.R
\name{classical_growth_interval}
\alias{classical_growth_interval}
\title{Classical Plant Growth Analysis by Two-Harvest Interval}
\usage{
classical_growth_interval(
  data,
  time = time,
  total_biomass = total_biomass_g,
  leaf_biomass = leaf_biomass_g,
  stem_biomass = stem_biomass_g,
  root_biomass = root_biomass_g,
  reproductive_biomass = reproductive_biomass_g,
  leaf_area = leaf_area_cm2,
  group_by = NULL,
  interval = NULL,
  ci_method = c("classical", "bootstrap", "both"),
  bootstrap_unit = c("row", "plant", "plot"),
  bootstrap_stratify = "treatment",
  n_boot = 499,
  conf_level = 0.95,
  seed = NULL,
  nar_label = c("ulr", "nar", "both"),
  allometry_method = c("lm", "bivariate_ml"),
  total_tolerance = 0.12,
  warn = TRUE
)
}
\arguments{
\item{data}{A data frame with harvest observations.}

\item{time}{Time or harvest-date column.}

\item{total_biomass}{Total biomass column.}

\item{leaf_biomass}{Leaf biomass column.}

\item{stem_biomass}{Stem biomass column.}

\item{root_biomass}{Root biomass column.}

\item{reproductive_biomass}{Optional reproductive biomass column.}

\item{leaf_area}{Leaf area column.}

\item{group_by}{Grouping columns (unquoted names or character vector).}

\item{interval}{Optional length-2 vector defining the two harvests.}

\item{ci_method}{One of \code{"classical"}, \code{"bootstrap"}, or \code{"both"}.}

\item{bootstrap_unit}{Bootstrap resampling unit (\code{"row"}, \code{"plant"}, \code{"plot"}).}

\item{bootstrap_stratify}{Optional stratification columns for bootstrap.}

\item{n_boot}{Number of bootstrap resamples.}

\item{conf_level}{Confidence level.}

\item{seed}{Optional random seed.}

\item{nar_label}{One of \code{"ulr"}, \code{"nar"}, or \code{"both"}.}

\item{allometry_method}{One of \code{"lm"} or \code{"bivariate_ml"}.}

\item{total_tolerance}{Relative tolerance for total biomass consistency checks.}

\item{warn}{Logical; emit warnings for non-fatal checks.}
}
\value{
An S3 object of class \code{phytogrow_classical} with components:
\itemize{
\item \code{results}: tibble with interval indices and uncertainty;
\item \code{input_data}: data used in analysis;
\item \code{checks}: tibble with warnings/issues;
\item \code{interval_info}: interval metadata;
\item \code{formulas}: list of formulas;
\item \code{ci_method}: selected CI method;
\item \code{reference}: Hunt et al. (2002) reference.
}
}
\description{
Compute classical interval growth indices from two successive harvests,
without mandatory plant pairing and without functional curve fitting.
}
\section{Interpretation}{

Reported indices are interval means between two harvests, not instantaneous
derivatives. For long time series with many harvests, functional approaches
may be more appropriate.
}

\section{Limitations}{

The method requires exactly two harvests per interval and positive values for
variables used in logarithmic transformations.
}

\examples{
fit_classic <- classical_growth_interval(
  data = hunt_classical_example,
  time = time,
  total_biomass = total_biomass_g,
  leaf_biomass = leaf_biomass_g,
  root_biomass = root_biomass_g,
  stem_biomass = stem_biomass_g,
  leaf_area = leaf_area_cm2,
  group_by = treatment,
  ci_method = "classical"
)
print(fit_classic)
summary(fit_classic)
tidy(fit_classic)
plot(fit_classic, metric = "rgr")

}
\references{
Hunt, R., Causton, D. R., Shipley, B., and Askew, A. P. (2002).
A modern tool for classical plant growth analysis.
\emph{Annals of Botany}, 90(4), 485-488. doi:10.1093/aob/mcf214
}
