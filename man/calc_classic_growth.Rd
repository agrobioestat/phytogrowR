% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_classic_growth.R
\name{calc_classic_growth}
\alias{calc_classic_growth}
\title{Calculate Classical Plant Growth Indices}
\usage{
calc_classic_growth(
  data,
  group_cols = c("treatment", "genotype", "block", "plot_id", "plant_id"),
  time_col = "time",
  biomass_col = "total_biomass_g",
  leaf_area_col = "leaf_area_cm2",
  leaf_mass_col = "leaf_biomass_g",
  stem_mass_col = "stem_biomass_g",
  root_mass_col = "root_biomass_g",
  ground_area_col = "ground_area_m2",
  epsilon = NULL,
  keep_intermediate = FALSE
)
}
\arguments{
\item{data}{A data frame in standardized format (see \code{\link[=prepare_growth_data]{prepare_growth_data()}}).}

\item{group_cols}{Grouping columns used to define independent trajectories.}

\item{time_col}{Name of the numeric time column.}

\item{biomass_col}{Name of total biomass column.}

\item{leaf_area_col}{Name of leaf area column.}

\item{leaf_mass_col}{Name of leaf biomass column.}

\item{stem_mass_col}{Name of stem biomass column.}

\item{root_mass_col}{Name of root biomass column.}

\item{ground_area_col}{Name of ground area column.}

\item{epsilon}{Optional positive numeric offset used in log transforms.}

\item{keep_intermediate}{Logical; keep intermediate columns when \code{TRUE}.}
}
\value{
A tibble with one row per interval and grouping unit.
}
\description{
Computes interval-based growth metrics between consecutive time points.
}
\details{
Implemented formulas (between consecutive times \code{t1} and \code{t2}):
\itemize{
\item \code{RGR = (log(W2) - log(W1)) / (t2 - t1)}
\item \code{AGR = (W2 - W1) / (t2 - t1)}
\item \code{NAR = ((W2 - W1)/(t2 - t1)) * ((log(LA2) - log(LA1))/(LA2 - LA1))}
\item \code{LAR = LA / W} (reported as interval mean of \code{t1} and \code{t2})
\item \code{SLA = LA / leaf_mass} (at \code{t1})
\item \code{LMR = leaf_mass / W}
\item \code{SMR = stem_mass / W}
\item \code{RMR = root_mass / W}
\item \code{CGR = (W2 - W1) / ((t2 - t1) * ground_area)}
\item \code{ALGR = (LA2 - LA1) / (t2 - t1)} (absolute leaf area growth rate)
\item \code{RLGR = (log(LA2) - log(LA1)) / (t2 - t1)} (relative leaf area growth rate)
\item \code{LAI = ((LA/10000) / ground_area)} (interval mean leaf area index)
\item \code{LAD = ((LA1 + LA2)/2) * (t2 - t1)} (leaf area duration in \verb{cm2 day})
}

Rates are interval averages, not instantaneous derivatives.
Classical concepts follow Hunt (1990), Benincasa (2003), and Hunt et al.
(2002) for interval-based growth analysis.
}
\examples{
dat <- prepare_growth_data(growth_wide_example)
classic <- calc_classic_growth(dat, group_cols = c("treatment", "plot_id", "plant_id"))
dplyr::glimpse(classic)
}
\references{
Hunt, R. (1990). \emph{Basic Growth Analysis}. Unwin Hyman, London.

Benincasa, M. M. P. (2003). \emph{Plant Growth Analysis: Basic Concepts}.
FUNEP, Jaboticabal.

Hunt, R., Causton, D. R., Shipley, B., and Askew, A. P. (2002).
A modern tool for classical plant growth analysis.
\emph{Annals of Botany}, 90(4), 485-488. doi:10.1093/aob/mcf214
}
