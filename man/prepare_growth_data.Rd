% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_growth_data.R
\name{prepare_growth_data}
\alias{prepare_growth_data}
\title{Prepare and Standardize Plant Growth Data}
\usage{
prepare_growth_data(
  data,
  mapping = list(),
  time_origin = NULL,
  group_cols = c("treatment", "genotype", "block", "plot_id", "plant_id"),
  derive_total = TRUE,
  quiet = TRUE
)
}
\arguments{
\item{data}{A data frame or tibble containing growth observations.}

\item{mapping}{Optional named list mapping original names to standard names.}

\item{time_origin}{Optional date used as day zero when \code{date} is available.
If \code{NULL}, the minimum date in the dataset is used.}

\item{group_cols}{Columns used to create \code{group_id}.}

\item{derive_total}{Logical; if \code{TRUE}, computes \code{total_biomass_g} from parts
when total biomass is missing.}

\item{quiet}{Logical; if \code{FALSE}, emits informative messages.}
}
\value{
A standardized tibble with a \code{group_id} column.
}
\description{
Standardize input datasets to the \code{phytogrowR} column convention, convert time
to numeric scale, build grouping identifiers, and optionally calculate total
biomass from biomass parts.
}
\details{
The standard output uses columns such as \code{time}, \code{total_biomass_g},
\code{leaf_biomass_g}, \code{leaf_area_cm2}, and \code{ground_area_m2}. Missing standard
columns are created with \code{NA} so downstream functions remain predictable.
}
\examples{
dat <- prepare_growth_data(growth_wide_example)
names(dat)
}
