% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_classical_growth.R
\name{bootstrap_classical_growth}
\alias{bootstrap_classical_growth}
\title{Bootstrap Confidence Intervals for Classical Interval Growth Indices}
\usage{
bootstrap_classical_growth(
  data,
  time = time,
  total_biomass = total_biomass_g,
  leaf_biomass = leaf_biomass_g,
  stem_biomass = stem_biomass_g,
  root_biomass = root_biomass_g,
  reproductive_biomass = reproductive_biomass_g,
  leaf_area = leaf_area_cm2,
  group_by = NULL,
  interval = NULL,
  resample_unit = c("row", "plant", "plot"),
  stratify_by = "treatment",
  n_boot = 499,
  conf_level = 0.95,
  seed = NULL,
  nar_label = c("ulr", "nar", "both"),
  allometry_method = c("lm", "bivariate_ml"),
  warn = FALSE
)
}
\arguments{
\item{data}{A data frame with harvest observations.}

\item{time}{Time or harvest-date column.}

\item{total_biomass}{Total biomass column.}

\item{leaf_biomass}{Leaf biomass column.}

\item{stem_biomass}{Stem biomass column.}

\item{root_biomass}{Root biomass column.}

\item{reproductive_biomass}{Optional reproductive biomass column.}

\item{leaf_area}{Leaf area column.}

\item{group_by}{Grouping columns (unquoted names or character vector).}

\item{interval}{Optional length-2 vector defining the two harvests.}

\item{resample_unit}{Resampling unit: \code{"row"}, \code{"plant"}, or \code{"plot"}.}

\item{stratify_by}{Optional stratification columns for bootstrap sampling.}

\item{n_boot}{Number of bootstrap resamples.}

\item{conf_level}{Confidence level.}

\item{seed}{Optional random seed.}

\item{nar_label}{One of \code{"ulr"}, \code{"nar"}, or \code{"both"}.}

\item{allometry_method}{One of \code{"lm"} or \code{"bivariate_ml"}.}

\item{warn}{Logical; emit warnings for non-fatal checks.}
}
\value{
A tibble with bootstrap uncertainty summaries for each index and group:
\code{estimate}, \code{standard_error}, \code{conf_low}, \code{conf_high}, and metadata.
}
\description{
Perform bootstrap resampling for the classical two-harvest module
(\code{classical_growth_interval()}), with support for row, plant, or plot
resampling and optional stratification.
}
\section{Interpretation}{

Bootstrap intervals reflect empirical sampling uncertainty in interval-average
indices under the chosen resampling scheme.
}

\section{Limitations}{

Unit-based bootstrap requires the corresponding id column (\code{plant_id} or
\code{plot_id}) to be present.
}

\examples{
boot_classic <- bootstrap_classical_growth(
  data = hunt_classical_example,
  time = time,
  total_biomass = total_biomass_g,
  leaf_biomass = leaf_biomass_g,
  root_biomass = root_biomass_g,
  stem_biomass = stem_biomass_g,
  leaf_area = leaf_area_cm2,
  group_by = treatment,
  resample_unit = "row",
  n_boot = 3,
  seed = 101
)
boot_classic

}
\references{
Hunt, R., Causton, D. R., Shipley, B., and Askew, A. P. (2002).
A modern tool for classical plant growth analysis.
\emph{Annals of Botany}, 90(4), 485-488. doi:10.1093/aob/mcf214
}
