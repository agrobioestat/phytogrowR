% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_root_shoot_allometry.R
\name{calc_root_shoot_allometry}
\alias{calc_root_shoot_allometry}
\title{Estimate Root-Shoot Allometric Coefficient}
\usage{
calc_root_shoot_allometry(
  root_biomass,
  shoot_biomass,
  method = c("lm", "bivariate_ml"),
  conf_level = 0.95
)
}
\arguments{
\item{root_biomass}{Root biomass vector (positive values required).}

\item{shoot_biomass}{Shoot biomass vector (positive values required).}

\item{method}{Either \code{"lm"} or \code{"bivariate_ml"}.}

\item{conf_level}{Confidence level.}
}
\value{
A one-row tibble with:
\code{intercept}, \code{beta}, \code{standard_error}, \code{conf_low}, \code{conf_high}, \code{p_value},
\code{r_squared}, \code{degrees_freedom}, \code{n}, and \code{method}.
}
\description{
Fit:
\deqn{log(root\_biomass) = alpha + beta * log(shoot\_biomass)}
}
\details{
The estimated \code{beta} is the root-shoot allometric coefficient.

Two methods are available:
\itemize{
\item \code{"lm"}: ordinary least squares on \code{log(root)} against \code{log(shoot)}.
\item \code{"bivariate_ml"}: a bivariate approximation using reduced major axis (RMA,
Model II) on log-transformed data, with jackknife-based standard error.
}

The \code{"bivariate_ml"} implementation is an approximation intended to be
compatible with classical allometric workflows where both axes may contain
measurement error.
}
\section{Interpretation}{

\code{beta > 1} suggests relatively faster root accumulation as shoot biomass
increases; \code{beta < 1} suggests the opposite.
}

\section{Limitations}{

At least five valid plants are required for stable estimation.
}

\examples{
calc_root_shoot_allometry(
  root_biomass = c(0.5, 0.7, 0.8, 1.1, 1.3, 1.6),
  shoot_biomass = c(1.2, 1.5, 1.6, 2.1, 2.4, 2.9),
  method = "lm"
)

}
\references{
Hunt, R., Causton, D. R., Shipley, B., and Askew, A. P. (2002).
A modern tool for classical plant growth analysis.
\emph{Annals of Botany}, 90(4), 485-488. doi:10.1093/aob/mcf214
}
