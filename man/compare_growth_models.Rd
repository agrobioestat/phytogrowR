% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_growth_models.R
\name{compare_growth_models}
\alias{compare_growth_models}
\title{Compare Candidate Growth Models}
\usage{
compare_growth_models(
  data,
  response = "total_biomass_g",
  time_col = "time",
  group_var = NULL,
  models = c("exponential", "logistic", "gompertz", "richards"),
  poly_degrees = 1:3,
  log_response = FALSE,
  epsilon = NULL,
  criterion = c("aic", "bic", "rmse")
)
}
\arguments{
\item{data}{A data frame with time, response and (optionally) grouping
columns.}

\item{response}{Response column, for example \code{"total_biomass_g"}.}

\item{time_col}{Numeric time column.}

\item{group_var}{Optional grouping column. When supplied, every candidate is
fitted and ranked within each group; when \code{NULL}, one ranking is produced
for the pooled data.}

\item{models}{Non-linear candidates among \code{"exponential"}, \code{"logistic"},
\code{"gompertz"} and \code{"richards"}.}

\item{poly_degrees}{Polynomial degrees to include as candidates. Use
\code{integer(0)} to drop them.}

\item{log_response}{Logical; fit every candidate to the natural log of the
response. Applied to all candidates alike, since criteria computed on
different response scales are not comparable.}

\item{epsilon}{Optional positive offset used before the log transform.}

\item{criterion}{Criterion used to flag the best candidate: \code{"aic"}
(default), \code{"bic"} or \code{"rmse"}.}
}
\value{
A tibble with one row per group and candidate, ordered by the chosen
criterion, with columns \code{model}, \code{converged}, \code{n_par}, \code{n_obs}, \code{rss},
\code{rmse}, \code{r_squared}, \code{aic}, \code{bic}, \code{delta} and \code{best}.
}
\description{
Fits several functional forms to the same data and ranks them, so the choice
between a logistic, a Gompertz, a Richards or a polynomial curve rests on
the data rather than on habit.
}
\details{
Every candidate is fitted to the same observations, on the same scale, with
Gaussian errors, which is what makes AIC and BIC comparable across the \code{lm}
and \code{nls} families. Candidates that fail to converge are kept in the output
with \code{converged = FALSE} rather than dropped, because a family failing to
converge is itself informative.

A word of caution on \code{r_squared}: for non-linear models it is not a genuine
coefficient of determination and cannot be used to compare models with
different numbers of parameters. Rank on \code{aic} or \code{bic}; read \code{r_squared}
only as a rough indication of fit.

\code{delta} is the difference to the best candidate on the chosen criterion.
A difference below about 2 is usually taken as "no meaningful distinction",
in which case the simpler or more interpretable family is the better choice.
}
\examples{
dat <- prepare_growth_data(growth_wide_example)
compare_growth_models(
  dat,
  response = "total_biomass_g",
  group_var = "treatment",
  models = c("logistic", "gompertz")
)
}
\seealso{
\code{\link[=compare_growth_curves]{compare_growth_curves()}} to test treatments once a form is chosen,
\code{\link[=fit_growth_curve]{fit_growth_curve()}} and \code{\link[=growth_curve_params]{growth_curve_params()}}.
}
